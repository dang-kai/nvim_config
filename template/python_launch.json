{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python debug",
            "type": "debugpy",
            "request": "launch",
            "stopOnEntry": true,
            "program": "${workspaceFolder}/test.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}
